// Package integration holds live-backend integration tests, gated behind the
// "integration" build tag so they never run with plain `go test ./...`.
// See integration/README.md.
package integration
